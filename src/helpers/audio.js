/** @type {(auto: HTMLAudioElement) => void} */
export const play = (audio) => {
  audio.play();
}

/** @type {(auto: HTMLAudioElement) => void} */
export const pause = (audio) => {
  audio.pause();
}